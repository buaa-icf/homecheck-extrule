diff --git a/node_modules/homecheck/lib/Index.d.ts b/node_modules/homecheck/lib/Index.d.ts
index d5166e1..023a627 100644
--- a/node_modules/homecheck/lib/Index.d.ts
+++ b/node_modules/homecheck/lib/Index.d.ts
@@ -1,5 +1,5 @@
 export { start, run } from './Main';
-export { BaseMetaData, BaseChecker } from './checker/BaseChecker';
+export { BaseMetaData, BaseChecker, AdviceChecker, isAdviceChecker } from './checker/BaseChecker';
 export * from './matcher/Matchers';
 export { CheckerStorage } from './utils/common/CheckerStorage';
 export { Rule } from './model/Rule';
diff --git a/node_modules/homecheck/lib/Index.js b/node_modules/homecheck/lib/Index.js
index bb7d4f1..89195f9 100644
--- a/node_modules/homecheck/lib/Index.js
+++ b/node_modules/homecheck/lib/Index.js
@@ -68,3 +68,9 @@ Object.defineProperty(exports, "Utils", { enumerable: true, get: function () { r
 var toolEntry_1 = require("./tools/toolEntry");
 Object.defineProperty(exports, "runTool", { enumerable: true, get: function () { return toolEntry_1.runTool; } });
 Object.defineProperty(exports, "Tools", { enumerable: true, get: function () { return toolEntry_1.Tools; } });
+// checkers (patched)
+var BaseChecker_1 = require("./checker/BaseChecker");
+Object.defineProperty(exports, "BaseMetaData", { enumerable: true, get: function () { return BaseChecker_1.BaseMetaData; } });
+Object.defineProperty(exports, "BaseChecker", { enumerable: true, get: function () { return BaseChecker_1.BaseChecker; } });
+Object.defineProperty(exports, "AdviceChecker", { enumerable: true, get: function () { return BaseChecker_1.AdviceChecker; } });
+Object.defineProperty(exports, "isAdviceChecker", { enumerable: true, get: function () { return BaseChecker_1.isAdviceChecker; } });
diff --git a/node_modules/homecheck/lib/checker/BaseChecker.d.ts b/node_modules/homecheck/lib/checker/BaseChecker.d.ts
index 74cdc8f..0a5e2d6 100644
--- a/node_modules/homecheck/lib/checker/BaseChecker.d.ts
+++ b/node_modules/homecheck/lib/checker/BaseChecker.d.ts
@@ -16,3 +16,8 @@ export interface BaseChecker {
     codeFix?(arkFile: ArkFile, fixKey: string): boolean;
     issues: IssueReport[];
 }
+export interface AdviceChecker extends BaseChecker {
+    beforeCheck(): void;
+    afterCheck(): void;
+}
+export declare function isAdviceChecker(checker: BaseChecker): checker is AdviceChecker;
diff --git a/node_modules/homecheck/lib/checker/BaseChecker.js b/node_modules/homecheck/lib/checker/BaseChecker.js
index 6fe0445..47c29c5 100644
--- a/node_modules/homecheck/lib/checker/BaseChecker.js
+++ b/node_modules/homecheck/lib/checker/BaseChecker.js
@@ -14,3 +14,7 @@
  * limitations under the License.
  */
 Object.defineProperty(exports, "__esModule", { value: true });
+exports.isAdviceChecker = isAdviceChecker;
+function isAdviceChecker(checker) {
+    return 'beforeCheck' in checker && 'afterCheck' in checker;
+}
diff --git a/node_modules/homecheck/lib/model/Defects.d.ts b/node_modules/homecheck/lib/model/Defects.d.ts
index 5a7b961..917709b 100644
--- a/node_modules/homecheck/lib/model/Defects.d.ts
+++ b/node_modules/homecheck/lib/model/Defects.d.ts
@@ -16,7 +16,8 @@ export declare class Defects {
     fixKey: string;
     showIgnoreIcon: boolean;
     engineName: string;
-    constructor(reportLine: number, reportColumn: number, endColumn: number, description: string, severity: number, ruleId: string, filePath: string, ruleDocPath: string, disabled: boolean, checked: boolean, fixable: boolean, showIgnoreIcon?: boolean);
+    methodName: string;
+    constructor(reportLine: number, reportColumn: number, endColumn: number, description: string, severity: number, ruleId: string, filePath: string, ruleDocPath: string, disabled: boolean, checked: boolean, fixable: boolean, methodName?: string, showIgnoreIcon?: boolean);
 }
 export declare class IssueReport {
     defect: Defects;
diff --git a/node_modules/homecheck/lib/model/Defects.js b/node_modules/homecheck/lib/model/Defects.js
index ae92866..5b1692f 100644
--- a/node_modules/homecheck/lib/model/Defects.js
+++ b/node_modules/homecheck/lib/model/Defects.js
@@ -5,7 +5,7 @@ exports.engine = {
     engineName: ''
 };
 class Defects {
-    constructor(reportLine, reportColumn, endColumn, description, severity, ruleId, filePath, ruleDocPath, disabled, checked, fixable, showIgnoreIcon = true) {
+    constructor(reportLine, reportColumn, endColumn, description, severity, ruleId, filePath, ruleDocPath, disabled, checked, fixable, methodName = "", showIgnoreIcon = true) {
         this.description = '';
         this.severity = -1; // 0:info, 1:warning, 2:error
         this.ruleId = '@perforce/<checker-name>';
@@ -22,6 +22,7 @@ class Defects {
         this.description = description;
         this.severity = severity;
         this.ruleId = ruleId;
+        this.methodName = methodName;
         this.fixKey = this.reportLine + '%' + this.reportColumn + '%' + endColumn + '%' + this.ruleId;
         this.mergeKey = filePath + '%' + this.fixKey + '%' + this.description;
         this.ruleDocPath = ruleDocPath;
diff --git a/node_modules/homecheck/lib/model/Project2Check.js b/node_modules/homecheck/lib/model/Project2Check.js
index 861ed8b..f13a3f3 100644
--- a/node_modules/homecheck/lib/model/Project2Check.js
+++ b/node_modules/homecheck/lib/model/Project2Check.js
@@ -60,6 +60,10 @@ class Project2Check {
     }
     collectMatcherCallbacks() {
         this.enabledRuleCheckerMap.forEach(checker => {
+            var BaseChecker_1 = require('../checker/BaseChecker');
+            if (BaseChecker_1.isAdviceChecker(checker)) {
+                checker.beforeCheck();
+            }
             const matcherCallbacks = checker.registerMatchers();
             matcherCallbacks.forEach(obj => {
                 const matcher = obj.matcher;
@@ -130,7 +134,11 @@ class Project2Check {
         }
     }
     collectIssues() {
+        var BaseChecker_1 = require('../checker/BaseChecker');
         this.enabledRuleCheckerMap.forEach((v, k) => {
+            if (BaseChecker_1.isAdviceChecker(v)) {
+                v.afterCheck();
+            }
             var _a;
             this.issues.push(...((_a = v.issues) === null || _a === void 0 ? void 0 : _a.reduce((acc, cur) => {
                 if (acc.some((item) => item.defect.mergeKey === cur.defect.mergeKey)) {
diff --git a/node_modules/homecheck/lib/utils/common/CheckEntry.js b/node_modules/homecheck/lib/utils/common/CheckEntry.js
index 1240143..d0dfa82 100644
--- a/node_modules/homecheck/lib/utils/common/CheckEntry.js
+++ b/node_modules/homecheck/lib/utils/common/CheckEntry.js
@@ -283,12 +283,17 @@ function buildScene(fileList, checkEntry) {
         checkEntry.scene = new arkanalyzer_1.Scene();
         checkEntry.scene.buildSceneFromFiles(sceneConfig);
         logger.info("Build scene completed.");
+    }
+    catch (error) {
+        logger.error("Build scene error: ", error);
+        return false;
+    }
+    try {
         checkEntry.scene.inferTypes();
         logger.info("Infer types completed.");
     }
     catch (error) {
-        logger.error("Build scene or infer types error: ", error);
-        return false;
+        logger.error("Infer types error, continue with partial scene: ", error);
     }
     // 构建Scope信息
     checkEntry.buildScope();
diff --git a/node_modules/homecheck/lib/utils/common/ConfigUtils.js b/node_modules/homecheck/lib/utils/common/ConfigUtils.js
index 89e8bec..7ed546c 100644
--- a/node_modules/homecheck/lib/utils/common/ConfigUtils.js
+++ b/node_modules/homecheck/lib/utils/common/ConfigUtils.js
@@ -165,7 +165,7 @@ class ConfigUtils {
                 return;
             }
             try {
-                const cmd = `${projectConfig.npmPath} install --no-save --prefix "${projectConfig.npmInstallDir}" "${ruleSet.packagePath}"`;
+                const cmd = `${projectConfig.npmPath} install --force --no-save --prefix "${projectConfig.npmInstallDir}" "${ruleSet.packagePath}"`;
                 logger.info('Start to execute cmd: ' + cmd);
                 const execLog = (0, child_process_1.execSync)(cmd);
                 logger.info('Exec log: ' + execLog.toString());
diff --git a/node_modules/homecheck/lib/utils/common/FileUtils.js b/node_modules/homecheck/lib/utils/common/FileUtils.js
--- a/node_modules/homecheck/lib/utils/common/FileUtils.js
+++ b/node_modules/homecheck/lib/utils/common/FileUtils.js
@@ -124,4 +124,6 @@ class FileUtils {
                 let readLineNo = 1;
+                const input = fs.createReadStream(filePath);
+                let stoppedEarly = false;
                 let rl = (0, readline_1.createInterface)({
-                    input: fs.createReadStream(filePath),
+                    input,
                     crlfDelay: Infinity
@@ -132,3 +134,5 @@ class FileUtils {
                             lines.push(line);
+                            stoppedEarly = true;
                             rl.close();
+                            input.destroy();
                         }
@@ -142,6 +146,14 @@ class FileUtils {
                 rl.on('close', () => {
-                    resolve(lines);
+                    if (stoppedEarly && !input.closed) {
+                        input.once('close', () => resolve(lines));
+                    }
+                    else {
+                        resolve(lines);
+                    }
                 });
                 rl.on('error', (err) => {
                     reject(err);
                 });
+                input.on('error', (err) => {
+                    reject(err);
+                });
diff --git a/node_modules/homecheck/lib/utils/common/FileUtils.js b/node_modules/homecheck/lib/utils/common/FileUtils.js
--- a/node_modules/homecheck/lib/utils/common/FileUtils.js
+++ b/node_modules/homecheck/lib/utils/common/FileUtils.js
@@ -94,14 +94,24 @@ class FileUtils {
         return __awaiter(this, void 0, void 0, function* () {
-            let result = [];
-            for (const filePath of fileList) {
-                if (!(ignoreGlob === null || ignoreGlob === void 0 ? void 0 : ignoreGlob.matchGlob(filePath)) && fileGlob.matchGlob(filePath)) {
-                    // 读取file文件内容首行，若为屏蔽行则跳过
-                    const firstLineText = yield this.readLinesFromFile(filePath, 1);
-                    if (firstLineText.includes(Disable_1.DisableText.FILE_DISABLE_TEXT)) {
-                        continue;
+            const candidates = fileList.filter((filePath) => !(ignoreGlob === null || ignoreGlob === void 0 ? void 0 : ignoreGlob.matchGlob(filePath)) && fileGlob.matchGlob(filePath));
+            const accepted = new Array(candidates.length).fill(false);
+            const configuredConcurrency = Number(process.env.HOMECHECK_FILE_CONCURRENCY || 64);
+            const concurrency = Number.isInteger(configuredConcurrency) && configuredConcurrency > 0
+                ? configuredConcurrency
+                : 64;
+            let nextIndex = 0;
+            const worker = () => __awaiter(this, void 0, void 0, function* () {
+                while (true) {
+                    const index = nextIndex++;
+                    if (index >= candidates.length) {
+                        return;
+                    }
+                    const firstLineText = yield this.readLinesFromFile(candidates[index], 1);
+                    if (!firstLineText.includes(Disable_1.DisableText.FILE_DISABLE_TEXT)) {
+                        accepted[index] = true;
                     }
-                    result.push(filePath);
                 }
-            }
-            return result;
+            });
+            const workers = Array.from({ length: Math.min(concurrency, candidates.length) }, () => worker());
+            yield Promise.all(workers);
+            return candidates.filter((_filePath, index) => accepted[index]);
         });
